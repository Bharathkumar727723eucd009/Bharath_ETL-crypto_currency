#!/bin/bash
# This script is a placeholder — the actual spark-submit runs
# from the spark-analytics container triggered by docker compose
echo "Spark analytics job triggered via Airflow"
echo "Check spark-analytics container logs for output"